server:
  port: 8086 # ? ??? ????? ?? ?? ??

spring:
  application:
    name: delivery-manager-service # ???? ? ???? ?????.

  # 1. ??? ??? ?? ?? (Spring Boot 3.5.x ?? ?? ??)
  cloud:
    compatibility-verifier:
      enabled: false

  # 2. ?? PostgreSQL ?? ?? (????? ?? ??? ???)
  datasource:
    url: jdbc:postgresql://localhost:54321/sparta_logistics?currentSchema=delivery_manager_schema
    username: sparta_admin
    password: password123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # 3. Redis ?? ??
  data:
    redis:
      host: localhost
      port: 6379

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # ??? ??? ?? ??