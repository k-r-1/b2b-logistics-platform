spring:
  profiles:
    active: local
  application:
    name: order-service
  cloud:
    compatibility-verifier:
      enabled: false
    openfeign:
      circuitbreaker:
        enabled: true


server:
  port: 8084