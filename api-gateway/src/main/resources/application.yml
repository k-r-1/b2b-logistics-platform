server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    compatibility-verifier:
      enabled: false
    gateway:
      routes:
        # 1. ?? ? ?? ?? ?? ?? ??
        - id: user-service
          uri: lb://user-service # ???? ??? ?? ??? ???? ????? ???
          predicates:
            - Path=/api/v1/auth/**, /api/v1/users/**


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # ??? ??? ? ?? ????